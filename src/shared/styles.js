import { StyleSheet } from 'react-native';

export const theme = StyleSheet.create({
  fullFlex: {
    flex: 1,
  },
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
